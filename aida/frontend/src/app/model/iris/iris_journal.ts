
export class IRISJournal {

    pid: string;
    issn: string;
    title: string

    constructor(pid, issn, title) {
        this.pid = pid
        this.issn = issn
        this.title = title
    }
}