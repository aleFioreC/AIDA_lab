export class People {
    name: string;
    surname: string;
    role: string;
    file: string;

    constructor(name, surname, role, file) {
        this.name = name
        this.surname = surname
        this.role = role
        this.file = file
    }
}